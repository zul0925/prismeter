# Prismeter 0.22.5

## Startup recovery hotfix

- Isolated startup rendering so a non-critical interface error cannot prevent application interactions.
- Bound native window controls independently of the page rendering lifecycle.
- Hardened language-selector initialization.
