## 中文

- 修复设置页布局错乱及设置自动保存失败的问题。
- 补齐更新检查、安装更新、窗口设置与退出应用所需的 Tauri 权限。
- 修复“退出 Prismeter”执行失败并显示 `undefined` 的问题。
- 移除暂不需要的“导出全部远端数据”功能。
- 修复自动更新清单的 UTF-8 BOM，保证更新器可以稳定解析 `latest.json`。

## English

- Fixed broken Settings layout and failed automatic preference saving.
- Added the required Tauri permissions for update checks, update installation, window settings, and application exit.
- Fixed “Exit Prismeter” failing with an `undefined` error.
- Removed the unnecessary “Export all remote data” action.
- Removed the UTF-8 BOM from the updater manifest so `latest.json` can be parsed reliably.
